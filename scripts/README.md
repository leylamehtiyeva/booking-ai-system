# Scripts

Utility scripts for local development, debugging, smoke checks, experiments, and evaluation support.

## Structure

- `dev/` — developer utilities
- `smoke/` — quick checks for core pipeline components
- `debug/` — manual debugging entry points
- `experiments/` — one-off exploratory scripts
- `evaluation/` — scripts used to run or inspect evaluation outputs

These scripts are not part of the production application path.